# DeepDive: Autonomous AI Research Agent 🌊

DeepDive is a stateful, full-stack AI research assistant. Powered by a LangGraph autonomous agent, it breaks down complex user queries, browses the live web for factual data, and synthesizes fully-cited, markdown-formatted reports in under 20 seconds. 

Unlike standard LLM wrappers, DeepDive utilizes memory checkpointers for iterative refinement and enforces enterprise-grade security using custom JWTs and Row Level Security (RLS).

**Live Demo:** [Insert Your Vercel URL Here]

## 🧠 System Architecture

DeepDive is built on a decoupled microservices architecture:

* **Frontend:** React + Vite, styled with custom CSS for a highly responsive dark-mode UI.
* **Backend:** FastAPI (Python) serving as the bridge to the LangGraph execution environment.
* **Agentic Engine:** LangGraph state machine utilizing Llama-3-70B (via Groq) and Tavily API for real-time web search.
* **Authentication:** Clerk (Email/OAuth integration).
* **Database:** Supabase (PostgreSQL) for persistent report storage and history tracking.

## ✨ Key Features

1. **Autonomous Multi-Step Reasoning:** The agent operates on a `Planner -> Researcher -> Writer` graph, ensuring data is sourced and fact-checked before generation.
2. **Stateful Memory (Iterative Refinement):** Utilizing LangGraph's `MemorySaver`, the agent remembers the context of the generated report, allowing users to ask follow-up questions or request specific paragraph rewrites seamlessly.
3. **Secure Data Persistence:** Frontend JWTs generated by Clerk are passed to Supabase, where strict Row Level Security (RLS) policies ensure users can only read, insert, and update their own historical data.
4. **Export Engine:** Custom client-side HTML-to-PDF rendering that strips UI elements and generates professional, light-mode document exports.

## 📂 Repository Structure (Monorepo)

## 📂 Repository Structure (Monorepo)

\'\'\' text
.
├── deepdive-frontend/       # React application (Vite)
│   ├── src/
│   │   ├── App.jsx          # Main UI, Auth logic, and API calls
│   │   ├── supabase.js      # Supabase client with Clerk JWT injection
│   │   └── main.jsx         # Clerk Provider wrapping
├── agent.py                 # LangGraph state machine and Nodes
├── main.py                  # FastAPI server and CORS security config
└── requirements.txt         # Backend Python dependencies
\'\'\'

## 🚀 Local Deployment

### 1. Clone the repository
\`\`\`bash
git clone https://github.com/yourusername/deepdive-research-agent.git
cd deepdive-research-agent
\`\`\`

### 2. Backend Setup (FastAPI + LangGraph)
\`\`\`bash
# Install dependencies
pip install -r requirements.txt

# Create a .env file in the root directory
GROQ_API_KEY=your_groq_key
TAVILY_API_KEY=your_tavily_key

# Run the backend server
uvicorn main:app --reload --port 8000
\`\`\`

### 3. Frontend Setup (React)
\`\`\`bash
cd deepdive-frontend
npm install

# Create a .env file in the frontend directory
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_key
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Start the development server
npm run dev
\`\`\`

## 🛡️ Security Implementations
* **API Protection:** FastAPI CORS middleware restricts traffic exclusively to the production Vercel domain.
* **Data Isolation:** Supabase RLS is enabled. Database queries require a valid Clerk JWT matching the row's `user_id`.
* **State Management:** Thread IDs are generated client-side and passed securely to the LangGraph memory checkpointer to prevent session bleeding.
